/**
 * Register Cloudflare Queue / Cron consumers.
 * Wire concrete handlers from `src/workers/*` when bindings are live.
 */
export function registerWorkers(): void {
  // Intentionally empty scaffold.
}
