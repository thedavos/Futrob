# `@futrob/sdk`

Cliente HTTP tipado para la API privada Futrob (`/api/v1`).

Depende solo de `@futrob/api-contracts`. No importa módulos de `apps/web`.

Guía general: [`/packages/README.md`](/packages/README.md).
